
    <% locals.userPosts.forEach(userPost => { %>
     
        <%= userPost.location %>

    <% }) %>

    <% for(let i = 0; i < userPosts.length; i++) { %>
        <%= userPosts[i].location %>
    <% } %>


    <%= userPosts[i].byUser.username %>

    <% if (userPosts.username) { %>
        <%= userPosts.username %> 
    <% } %> 


    <% if (serverMsg) { %>
        <p class="<%= serverMsg.type %>"><%= serverMsg.message %></p>
      <% } %> 